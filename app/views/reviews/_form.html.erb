<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit 'Review!', class: "btn btn-success"%>

<% end %>
